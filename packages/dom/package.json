{
  "name": "@mithic/dom",
  "version": "0.3.0",
  "description": "Virtual DOM API for mithic",
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./root": "./dist/root.js"
  },
  "files": [
    "/dist"
  ],
  "scripts": {
    "prepublishOnly": "npm run clean && npm run build && npm test && npm run doc",
    "clean": "rimraf docs dist *.lcov",
    "prebuild": "npm run lint",
    "build": "npm run babel",
    "postbuild": "npm run tsc",
    "lint": "eslint",
    "babel": "babel src -d dist -x '.js,.ts' --root-mode upward",
    "tsc": "tsc --project tsconfig.build.json",
    "test": "COLORS=false node --experimental-strip-types --experimental-test-snapshots --experimental-test-coverage --test-coverage-include=\"src/**/*.ts\" --test-coverage-exclude=\"**/*.test.ts\" --test-reporter=spec --test-reporter-destination=stdout --test-reporter=lcov --test-reporter-destination=coverage.lcov --test \"**/*.test.ts\"",
    "test:update": "COLORS=false node --experimental-strip-types --experimental-test-snapshots --test-update-snapshots --test \"**/*.test.ts\"",
    "doc": "typedoc"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/andykswong/mithic.git"
  },
  "keywords": [
    "dom",
    "mithic",
    "typescript"
  ],
  "author": "Andy K.S. Wong <andykswong@outlook.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/andykswong/mithic/issues"
  },
  "homepage": "https://github.com/andykswong/mithic",
  "dependencies": {
    "@mithic/commons": "^0.3"
  },
  "devDependencies": {
  }
}

[package]
name = "mithic_curl"
edition = "2024"
publish = false

[dependencies]
wit-bindgen = { version = "0.57" }

[package.metadata.component]
package = "mithic:curl"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"
